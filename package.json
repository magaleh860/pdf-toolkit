{
  "name": "pdf-toolkit",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "packageManager": "pnpm@8.8.0",
  "engines": {
    "node": ">=16"
  },
  "scripts": {
    "bootstrap": "pnpm install",
    "build": "pnpm -w -r build",
    "test": "pnpm -w -r test"
  }
}
